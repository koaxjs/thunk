{
  "name": "@koax/thunk",
  "description": "Koax thunk middleware.",
  "repository": "git://github.com/koaxjs/thunk.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-function": "^1.1.1",
    "@f/thunk-to-promise": "^1.1.1"
  },
  "devDependencies": {
    "@f/is-promise": "^1.1.1",
    "babel-cli": "^6.4.0",
    "babel-polyfill": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "tape": "^4.2.2"
  }
}
